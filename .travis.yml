language: rust
rust:
    - stable
script:
    - cargo build --verbose --all
    - cargo test --verbose --tests
    - cargo doc
deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    keep-history: true
    local_dir: site
